import { Button } from "@/components/ui/button";

const NullColumn = () => {
    return ( 
        <Button className="w-full">
            Criar lista de tarefas
        </Button>
    );
};
 
export default NullColumn;